#!/bin/bash
# This could be run as a background process to parallel
RUNTIME=${RUNTIME:-io.containerd.runc.v2}
NAME=${NAME:-dummy}
SNAME=${SNAME:-$NAME-server}
CNAME=${CNAME:-$NAME}
ROUNDS=${ROUNDS:-1}
MEM=${MEM:-4G}
REALMEM=$(echo "$MEM" | cut -d G -f 1)
REALMEM="$((REALMEM + 1))G"
NSERVERS=${NSERVERS:-4}
NCLIENTS=${NCLIENTS:-4}

# Server Side Initialization (Just use the temporary filesystem)
nerdctl run -d \
  $EXTRA_SERVER_FLAGS \
  --runtime "$RUNTIME" \
  -m "$REALMEM" \
  --name "$SNAME" \
  --cpus "$NSERVERS" \
  $SCPUSET \
  --tmpfs /tmp:size=$MEM \
  "toolmanp/ioperf-dockerized:22.04-zpoline" \
  bash -c "mv /var/lib/mysql /tmp/mysql && ${ZRUN:-} mysqld  --bind-address=0.0.0.0 --init-file=/ioperf/mysqld-init.sql --datadir=/tmp/mysql" &> /dev/null

sleep 10

IP=$(nerdctl inspect --format '{{.NetworkSettings.IPAddress}}' "$SNAME")
# Client Side Benchmarking (Use native runc)
for _ in $(seq 1 "$ROUNDS"); do
nerdctl run \
  --rm \
  --name "$CNAME" \
  $CCPUSET \
  -v $ROOT/bin/barrier:/barrier \
  "toolmanp/go-ycsb-rocksdb-dockerized:22.04-zpoline" \
  bash -c "$SYNC_PRI export workloads=\"a b c d e f\"; for w in \$workloads; do go-ycsb run mysql -p recordcount=50000 -p operationcount=100000 -p threadcount="$NCLIENTS" -p mysql.host="$IP" -p mysql.password=root -P /workloads/workload\$w; done" | tee -a "$LOGS/$CNAME".log

done

sleep 1

nerdctl rm --force "$SNAME" &> /dev/null
